# Local design/development artifacts — reference renders, comparison screenshots,
# measurement passes and headless-browser profiles. Kept on disk, out of the repo.
dev/

# OS / editor noise
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store
.vscode/
.idea/
*.swp

# Dependency / build output (none today, guarded for later)
node_modules/
dist/
build/
*.log
